#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_biased-opener@0.2.8@biased-opener/bin/biased-opener.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_biased-opener@0.2.8@biased-opener/bin/biased-opener.js" "$@"
  ret=$?
fi
exit $ret
