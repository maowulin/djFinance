#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_x-default-browser@0.3.1@x-default-browser/bin/x-default-browser.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_x-default-browser@0.3.1@x-default-browser/bin/x-default-browser.js" "$@"
  ret=$?
fi
exit $ret
